name: blank
on:
  push:
jobs:
  blank:
    runs-on: ubuntu-latest
    steps:
      - name: MyCommands
        run: |
          RED='\033[0;31m'
          YELLOW='\033[1;33m'
          NC='\033[0m' # No Color
          printf "I ${RED}love${YELLOW} Stack ${NC}Overflow\n"
